# Bundled observability off - platform stack (kube-prometheus-stack,
# otel-operator collector) provides metrics/dashboards.
opentelemetry-collector:
  enabled: false
jaeger:
  enabled: false
prometheus:
  enabled: false
grafana:
  enabled: false
opensearch:
  enabled: false
